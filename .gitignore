node_modules
dist
.DS_Store

# local config — may hold an API key (legacy; prod uses .env)
config.js
config.local.js

# monitor site list (deployer-edited; commit sites.example.json instead)
sites.json

# environment
.env

# sqlite database + WAL sidecars
*.db
*.db-shm
*.db-wal

# legacy runtime logs / files
llm.log
worlds/
